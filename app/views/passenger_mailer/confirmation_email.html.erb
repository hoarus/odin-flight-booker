<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Your flight has been booked, <%= @passenger.name %></h1>
    <h2>Your Flight Details</h2>
      <table>
        <tr>
          <th>Departure Airport</th>
          <th>Arrival Airport</th>
          <th>Departure Date</th>
          <th>Departure Time</th>
          <th>Duration</th>
          <th>Arrival Date</th>
          <th>Arrival Time</th>
        </tr>
        <tr>
          <td><%= @booking.flight.departure_airport.city %></td>
          <td><%= @booking.flight.arrival_airport.city %></td>
          <td><%= @booking.flight.start_datetime.strftime("%d %b, %Y") %></td>
          <td><%= @booking.flight.start_datetime.strftime("%H:%M") %></td>
          <td><%= @booking.flight.duration.strftime("%H:%M") %></td>
          <td><%= (@booking.flight.start_datetime + @booking.flight.duration).strftime("%d %b, %Y") %></td>
          <td><%= (@booking.flight.start_datetime + @booking.flight.duration).strftime("%H:%M") %></td>
        </tr>
      </table>
    <p>We hope you enjoy your flight!</p>
  </body>
</html>